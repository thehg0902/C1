Deploy the site via Hostinger Git integration.

Preconditions (verify, do not assume): phase 6 qa = done in
agency/state/BUILD_STATE.md; working tree committed; human has confirmed deploy
in this session. Then follow the deploy-hostinger skill exactly. After
deploy: record live URL + date in BUILD_STATE.md, run the post-deploy
checks from the skill.
