Maintain the Agency OS itself.

1. Run `python3 agency/scripts/lint-skills.py` and fix every violation it reports.
2. Run `python3 agency/scripts/generate-skill-registry.py` to refresh agency/docs/REGISTRY.md.
3. Report a one-line summary per fixed violation.
