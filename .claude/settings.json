{
  "$comment": "VERIFY hook wiring format against current Claude Code hooks docs before first use (see README).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "bash .claude/hooks/pre-commit-secrets.sh" },
          { "type": "command", "command": "bash .claude/hooks/pre-deploy-qa-gate.sh" }
        ]
      }
    ]
  }
}
